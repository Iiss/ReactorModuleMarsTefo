package elements 
{
	/**
	 * ...
	 * @author liss
	 */
	public class LinearGraph 
	{
		
		public function LinearGraph() 
		{
			/*
			 import flash.events.Event;
import flash.geom.Rectangle;

stop();

var valueStack:Array;
var value:Number = 0;
var angle:Number = 0;
var minValue:Number = 0;
var maxValue:Number = 0;

var plane:Rectangle = new Rectangle(0,0,320,240);

function f(x:Number):Number{return Math.sin(x);}

function degToRad(val:Number):Number{ return val*Math.PI/180;}

function init(resolution:int):void
{
	valueStack = new Array();
	
	for(var i:int=0;i<resolution;i++)
	{
		valueStack.push(0);
	}
}

function update(e:Event):void
{
	angle++;
	value=f(degToRad(angle));
	
	valueStack.push(value);
	valueStack.shift();
	
	minValue = Math.min(minValue,value);
	maxValue = Math.max(maxValue,value);
	
	redrawGraph();
}

function redrawGraph():void
{
	graphics.clear();
	graphics.lineStyle(2,0xE8D800);
	
	var delta:Number = maxValue-minValue;
}



init(32);
stage.addEventListener(Event.ENTER_FRAME,update)
			 * */
		}
		
	}

}